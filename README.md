# Pet Website

This is the static website for Pet.

## Structure

- `index.html`: Main landing page.
- `style.css`: Styles and animations.
- `script.js`: Interactivity (scroll reveal, mobile menu).

## Deployment

This folder is ready to be deployed to GitHub Pages.
Ensure your GitHub Actions workflow deploys the `website` folder content to the `gh-pages` branch.
